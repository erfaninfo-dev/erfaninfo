# راهنمای راه‌اندازی سیستم امنیتی ثبت نام

## ویژگی‌های جدید اضافه شده

### 🔒 امنیت رمز عبور
- **هش کردن با PBKDF2**: رمزهای عبور با الگوریتم PBKDF2 هش می‌شوند (جایگزین bcrypt)
- **بررسی قدرت رمز**: حداقل 8 کاراکتر، شامل حروف و عدد
- **نمایش قدرت رمز**: نوار پیشرفت قدرت رمز در فرانت‌اند

### 📧 اعتبارسنجی ایمیل
- **ایمیل الزامی**: ایمیل اکنون فیلد اجباری است
- **بررسی فرمت**: اعتبارسنجی فرمت ایمیل
- **یکتایی**: بررسی یکتا بودن ایمیل

### 🛡️ Rate Limiting
- **محدودیت درخواست**: حداکثر 5 درخواست ثبت نام در دقیقه
- **محافظت از brute force**: جلوگیری از حملات brute force

### ✅ قوانین و شرایط
- **Checkbox اجباری**: کاربر باید قوانین را بپذیرد
- **ذخیره زمان پذیرش**: زمان پذیرش قوانین ذخیره می‌شود

### 🔐 Google OAuth (اختیاری)
- **قابل اضافه کردن**: کد آماده برای اضافه کردن Google OAuth
- **نیاز به کتابخانه‌های اضافی**: برای فعال‌سازی نیاز به نصب کتابخانه‌های Google

## مراحل نصب

### 1. نصب کتابخانه‌ها
```bash
pip install -r requirements.txt
```

### 2. تنظیم متغیرهای محیطی
فایل `.env` را ایجاد کنید:
```env
# تنظیمات دیتابیس MySQL
MYSQL_HOST=localhost
MYSQL_USER=your_username
MYSQL_PASSWORD=your_password
MYSQL_DB=your_database

# تنظیمات Google OAuth
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here

# تنظیمات امنیتی
SECRET_KEY=your_secret_key_here
FLASK_ENV=development
```

### 3. آپدیت دیتابیس
```bash
mysql -u your_username -p your_database < update_user_table_security.sql
```

### 4. تنظیم Google OAuth

#### الف) ایجاد پروژه در Google Cloud Console
1. به [Google Cloud Console](https://console.cloud.google.com/) بروید
2. پروژه جدید ایجاد کنید یا پروژه موجود را انتخاب کنید
3. API "Google+ API" را فعال کنید

#### ب) ایجاد OAuth 2.0 credentials
1. به بخش "Credentials" بروید
2. "Create Credentials" > "OAuth 2.0 Client IDs" را انتخاب کنید
3. نوع "Web application" را انتخاب کنید
4. Authorized JavaScript origins اضافه کنید:
   - `http://localhost:5000` (برای development)
   - `https://yourdomain.com` (برای production)
5. Client ID و Client Secret را کپی کنید

#### ج) تنظیم در کد
در فایل `templates/auth.html` خط 282 را آپدیت کنید:
```javascript
client_id: 'YOUR_ACTUAL_GOOGLE_CLIENT_ID',
```

### 5. تنظیم SSL (برای Production)

#### الف) استفاده از Let's Encrypt
```bash
# نصب certbot
sudo apt install certbot python3-certbot-nginx

# دریافت گواهی SSL
sudo certbot --nginx -d yourdomain.com
```

#### ب) تنظیم Flask برای HTTPS
```python
if __name__ == '__main__':
    app.run(ssl_context='adhoc', host='0.0.0.0', port=443)
```

### 6. تست سیستم

#### الف) تست ثبت نام معمولی
1. به صفحه ثبت نام بروید
2. ایمیل معتبر وارد کنید
3. رمز عبور قوی وارد کنید
4. قوانین را بپذیرید
5. ثبت نام کنید

#### ب) تست Google OAuth
1. دکمه "ادامه با Google" را کلیک کنید
2. با حساب Google وارد شوید
3. اطلاعات کاربری بررسی شود

#### ج) تست Rate Limiting
1. چندین بار سریع ثبت نام کنید
2. باید محدودیت اعمال شود

## نکات امنیتی مهم

### 🔐 رمزهای عبور
- **هرگز** رمزهای عبور را در لاگ‌ها ذخیره نکنید
- از رمزهای قوی برای دیتابیس استفاده کنید
- به‌طور منظم رمزهای عبور را تغییر دهید

### 🌐 SSL/HTTPS
- **حتماً** از HTTPS در production استفاده کنید
- گواهی SSL را به‌طور منظم تمدید کنید
- HSTS header را فعال کنید

### 🛡️ Rate Limiting
- محدودیت‌ها را بر اساس نیاز تنظیم کنید
- IP های مشکوک را بلاک کنید
- لاگ‌های امنیتی را بررسی کنید

### 📊 مانیتورینگ
- لاگ‌های ورود ناموفق را بررسی کنید
- تلاش‌های ثبت نام مشکوک را رصد کنید
- آمار استفاده از Google OAuth را بررسی کنید

## عیب‌یابی

### مشکل: Google OAuth کار نمی‌کند
**راه حل:**
1. Client ID را بررسی کنید
2. Domain را در Google Console اضافه کنید
3. Console browser را برای خطاها بررسی کنید

### مشکل: Rate limiting خیلی سخت است
**راه حل:**
در `app.py` خط 449 را آپدیت کنید:
```python
@limiter.limit("10 per minute")  # افزایش محدودیت
```

### مشکل: رمز عبور هش نمی‌شود
**راه حل:**
1. کتابخانه bcrypt را بررسی کنید
2. Python version را بررسی کنید (3.6+)

## پشتیبانی
برای سوالات و مشکلات، لطفاً issue ایجاد کنید یا با تیم توسعه تماس بگیرید.
