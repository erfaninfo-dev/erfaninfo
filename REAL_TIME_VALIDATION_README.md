# سیستم اعتبارسنجی Real-Time

## ✨ ویژگی‌های جدید اضافه شده

### 🔍 اعتبارسنجی Real-Time
- **نام کاربری**: بررسی طول، کاراکترهای مجاز و یکتا بودن
- **ایمیل**: بررسی فرمت و یکتا بودن
- **رمز عبور**: نمایش قدرت رمز و راهنمایی
- **نام نمایشی**: بررسی طول

### 🎨 نمایش خطاها
- **خطاهای قرمز**: در زیر هر فیلد نمایش داده می‌شود
- **حاشیه قرمز**: فیلدهای دارای خطا
- **حاشیه سبز**: فیلدهای معتبر
- **پیام‌های راهنما**: توضیح دقیق مشکل

### ⚡ عملکرد
- **بررسی با تاخیر**: 500ms بعد از تایپ متوقف شدن
- **بررسی سرور**: برای یکتا بودن نام کاربری و ایمیل
- **اعتبارسنجی محلی**: برای فرمت و طول

## 🚀 نحوه کارکرد

### 1. نام کاربری
```
✅ معتبر: "john_doe123"
❌ نامعتبر: "jo" (کوتاه)
❌ نامعتبر: "john@doe" (کاراکتر غیرمجاز)
❌ نامعتبر: "admin" (تکراری)
```

### 2. ایمیل
```
✅ معتبر: "user@example.com"
❌ نامعتبر: "invalid-email" (فرمت نادرست)
❌ نامعتبر: "existing@example.com" (تکراری)
```

### 3. رمز عبور
```
✅ قوی: "MyPass123!" (8+ کاراکتر، حروف، عدد، کاراکتر خاص)
⚠️ متوسط: "mypass123" (8+ کاراکتر، حروف، عدد)
❌ ضعیف: "123456" (فقط عدد)
```

## 🔧 API Endpoints جدید

### `/check-username` (POST)
```json
{
  "username": "john_doe"
}
```
**پاسخ:**
```json
{
  "available": true,
  "message": "نام کاربری در دسترس است"
}
```

### `/check-email` (POST)
```json
{
  "email": "user@example.com"
}
```
**پاسخ:**
```json
{
  "available": true,
  "message": "ایمیل در دسترس است"
}
```

## 🎯 مزایا

1. **تجربه کاربری بهتر**: کاربر بلافاصله متوجه مشکل می‌شود
2. **کاهش خطاها**: قبل از ارسال فرم مشکلات برطرف می‌شوند
3. **راهنمایی دقیق**: پیام‌های خطا مشخص و مفید
4. **عملکرد بهتر**: بررسی‌های محلی سریع‌تر از سرور

## 📱 نمونه‌های پیام‌های خطا

### نام کاربری
- "نام کاربری باید حداقل 3 کاراکتر باشد"
- "نام کاربری فقط می‌تواند شامل حروف، عدد و _ باشد"
- "این نام کاربری قبلاً استفاده شده است"

### ایمیل
- "فرمت ایمیل نامعتبر است"
- "این ایمیل قبلاً ثبت شده است"

### رمز عبور
- "رمز عبور ضعیف است - حداقل 8 کاراکتر، حروف کوچک، حروف بزرگ، عدد"
- "رمز عبور متوسط است - برای امنیت بیشتر حروف بزرگ، کاراکتر خاص اضافه کنید"

## 🔄 جریان کار

1. **کاربر شروع به تایپ می‌کند**
2. **اعتبارسنجی محلی** (فرمت، طول)
3. **تاخیر 500ms** (برای جلوگیری از درخواست‌های زیاد)
4. **بررسی سرور** (یکتا بودن)
5. **نمایش نتیجه** (خطا یا موفقیت)

## 🛠️ تنظیمات

### تغییر تاخیر بررسی
```javascript
// در templates/auth.html خط 240
usernameTimeout = setTimeout(async () => {
  // بررسی نام کاربری
}, 500); // تغییر این عدد (میلی‌ثانیه)
```

### تغییر پیام‌های خطا
```javascript
// در templates/auth.html
showFieldError('su-username', 'پیام خطای جدید');
```

## ✅ تست

برای تست سیستم:
1. نام کاربری تکراری وارد کنید
2. ایمیل نامعتبر تایپ کنید
3. رمز عبور ضعیف بنویسید
4. مشاهده کنید که خطاها بلافاصله نمایش داده می‌شوند

## 🎉 نتیجه

سیستم ثبت نام شما حالا دارای:
- ✅ اعتبارسنجی real-time
- ✅ نمایش خطاهای دقیق
- ✅ راهنمایی کاربر
- ✅ تجربه کاربری بهتر
- ✅ کاهش خطاهای ثبت نام
